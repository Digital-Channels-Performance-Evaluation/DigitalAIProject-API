backend/data/processed/
backend/data/raw/
backend/data/validation/
# Python
backend/app/__pycache__/
backend/app/api/__pycache__/
backend/app/core/__pycache__/
backend/alembic/__pycache__/


*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
venv/
# Ignore committed venv directories (fix: backend/venv was committed)
backend/venv/
*.db

# Node
frontend/node_modules/
frontend/.build/
frontend/.build/cache/
.next/
.nuxt/
frontend/dist/
npm-debug.log*
# Next.js build output
frontend/.build/

# Environment — NEVER commit secrets
.env
.env.local
.env.*.local
# Keep .env.example (template with no real secrets)
!.env.example

# ML Models (large binaries - store in artifact storage)
backend/ml_models/

# Alembic ini is project-specific config, keep it tracked
# but never store real credentials in it
# backend/alembic.ini  <-- keep tracked

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker volumes (local dev)
mysql_data/
redis_data/
celery_beat_data/

# SSL certs (never commit)
nginx/ssl/*.pem
nginx/ssl/*.crt
nginx/ssl/*.key

# Celery beat schedule file
celerybeat-schedule
celerybeat.pid

# pytest temp files
test_ahadu.db
test.db
